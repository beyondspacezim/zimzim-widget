# zimzim-widget

미니창고짐 AI 챗봇 위젯 배포용 정적 파일 (GitHub Pages 호스팅).

- `widget.js` — 아임웹 'HTML 삽입' 블록에 임베드되는 번들 (React+Three.js+CSS 포함)
- 소스코드: private 레포 `zimzim-chatbot`의 `react-widget/` — 수정 후 `npm run build` 결과물을 이 레포에 복사·push하면 반영
